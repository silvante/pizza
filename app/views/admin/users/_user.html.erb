<div class="bg-white rounded-lg border">
    <div class="p-4 flex justify-between items-center">
        <div>
            <strong>ism:</strong>
            <%= link_to admin_user_path(user) do %>
                <h1 class="hover:underline"><%= user.name %></h1>
            <% end %>
        </div>
        <div>
            <strong>nomer:</strong>
            <h1>+998 <%= user.mobile %></h1>
        </div>
        <div>
            <strong>jami buyurtmalari:</strong>
            <% ordered_orders = user.order.select { |order| order.status == "delivered" } %>
            <h1 class="font-semibold <%= ordered_orders.length < 0 ? "text-green-600" : "text-red-600" %>"><%= ordered_orders.length %> ta</h1>
        </div> 
    </div>
    <div class="p-4 border-t flex items-center justify-between">
        <p><strong>qoshilgan sana:</strong> <%= user.created_at %></p>
        <%= link_to admin_user_path(user) do %>
            <p class="py-2 px-4 bg-base text-white rounded-lg">Mijozni organish</p>
        <% end %>
    </div>
</div>